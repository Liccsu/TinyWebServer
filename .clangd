# clangd 配置：检查集与 .clang-tidy 完全对齐（Add 的 modernize-* 会覆盖
# .clang-tidy 的排除项，因此 Remove 必须镜像全部排除，否则编辑器与 CI 诊断不一致）
CompileFlags:
  # 编译数据库目录（相对项目根）；Windows 编辑器亦可用
  CompilationDatabase: build

Diagnostics:
  ClangTidy:
    Add:
      - bugprone-*
      - clang-analyzer-*
      - performance-*
      - modernize-*
    # 镜像 .clang-tidy 的排除项（按检查集分类）
    Remove:
      # modernize：风格偏好，保持传统前置返回类型
      - modernize-use-trailing-return-type
      - modernize-use-nodiscard
      - modernize-avoid-c-arrays
      # 系统 API 固有模式，非误用
      - bugprone-easily-swappable-parameters
      - cppcoreguidelines-no-malloc
      - bugprone-multi-level-implicit-pointer-conversion
